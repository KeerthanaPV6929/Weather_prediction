# Weather_prediction
A simple Python weather app that fetches real-time weather data from the OpenWeatherMap API. Users enter a city name, and the script retrieves and displays the current temperature and weather description. A companion script (climate.py) handles reading the API key from a local file for later reuse.
